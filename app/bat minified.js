const display = require("display"), wifi = require("wifi"), storage = require("storage"), dialog = require("dialog"), serverUrl = "https://bat-five.vercel.app/"; function clear() { display.fill(display.color(0, 0, 0)) } function splash() { display.setTextAlign(1, 1), display.drawText("BAT Package Manager", 50, 50), delay(2000), clear() } function startup() { -1 == storage.readdir("/").indexOf(".bat") && storage.mkdir(".bat") } function installMenu() { clear(); var e = dialog.choice(["Search", "Browse", "Back"]); "Search" == e && (clear(), dialog.message("Not yet implemented", !0)), "Browse" == e && (clear(), dialog.message("Not yet implemented", !0)) } clear(), startup(), wifi.connected() || wifi.connectDialog(); const menuOptions = ["Manage", "Install", "Remove", "Infos", "Cancel"]; for (var selected = dialog.choice(menuOptions); "Cancel" != selected;) { "Manage" == selected && (clear(), dialog.message("Not yet implemented", !0)), "Install" == selected && installMenu(), "Remove" == selected && (clear(), dialog.message("Not yet implemented", !0)), "Infos" == selected && (clear(), dialog.message("BAT\nBruce Application Toolkit - A packet manager for Bruce firmware", !0)); var selected = dialog.choice(menuOptions) }